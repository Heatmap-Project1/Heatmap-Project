# Heatmap-Project
<p>
used library PIL for overlay generation -- Image.new() for blank image; image.putpixel(loc,col); PIL.Image.blend(IM1, IM2, alpha) -- formula is im1(1-alpha) + im2(alpha) -- for overlaying pixeldata

used requests to fetch data from weatherapi.com;

pandas used for data manipulation

used Raylib for making spinning earth graphic

used matplotlib mainly for colormaps and plotting
</p>